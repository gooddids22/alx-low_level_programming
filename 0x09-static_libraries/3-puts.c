#include "main.h"
#include <stdio.h>

/**
* main - Entry point
* _puts - function that inputs character to string
*
* Return: Always 0 (Success)
*/

void _puts(char *str)

int main(void);

{

_puts("\"Programming is like building a multilingual puzzle");
return 0;
}
